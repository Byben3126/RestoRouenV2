// interfaces/user.interface.ts
export interface IUser {
id: string;
email: string;
name: string;
}

purrement typage

Les interfaces définissent "à quoi doivent ressembler les données" sans dire "comment les créer ou les valider".
